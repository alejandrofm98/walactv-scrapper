FROM python:3.11-slim

WORKDIR /app

# Instalar dependencias para el proxy
RUN pip install flask requests gunicorn gevent

# Copiar el script del proxy
COPY scripts/acestream_proxy.py /app/scripts/

EXPOSE 8000

CMD ["python", "scripts/acestream_proxy.py"]