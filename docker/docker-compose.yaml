services:
  walactv-proxificaurl:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    command: [ "python", "proxificaUrl.py" ]
    env_file:
      - path: .env

  walactv-new-scrapper:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    command: [ "python", "main2.py" ]
    env_file:
      - path: .env

  walactv-futboltv:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    command: [ "python", "main.py" ]
    env_file:
      - path: .env

  #PARA LOCAL
#  acestream-http-proxy:
#    image: ghcr.io/martinbjeldbak/acestream-http-proxy
#    container_name: acestream-http-proxy
#    ports:
#      - 6878:6878
#    environment:
#      - ALLOW_REMOTE_ACCESS=yes

  acestream-arm:
    image: jopsis/acestream:arm64
    container_name: acestream-arm
    privileged: true
    restart: unless-stopped
    platform: linux/arm64
    hostname: acestream-arm
    ports:
      - "6878:6878"
    environment:
      TZ: Europe/Madrid
      ALLOW_REMOTE_ACCESS: "yes"
      ACESTREAM_CACHE_SIZE: "512M"
      ACESTREAM_MAX_PEERS: "20"
      ACESTREAM_DEBUG: "1"
      ACESTREAM_MAX_CONNECTIONS: "100"
      ACESTREAM_MAX_UPLOAD_SLOTS: "5"
      ACESTREAM_UPLOAD_RATE_LIMIT: "1M"
      ACESTREAM_DOWNLOAD_RATE_LIMIT: "0"
      ACESTREAM_LIVE_BUFFER_SIZE: "20"
      ACESTREAM_DISK_CACHE_LIMIT: "1024M"
      ACESTREAM_MEMORY_CACHE_LIMIT: "512M"
      ACESTREAM_PORT: "6878"
      ACESTREAM_STATE_DIR: "/dev/shm/.ACEStream"
      ACESTREAM_VOD_BUFFER: "15"
      ACESTREAM_LIVE_CACHE_TYPE: "memory"
      # CRÍTICO: Timeout de sesiones inactivas
      ACESTREAM_SESSION_TIMEOUT: "300"  # 5 minutos
      ACESTREAM_CLIENT_TIMEOUT: "180"   # 3 minutos
    volumes:
      - acestream-config:/acestream.engine
    tmpfs:
      - /dev/shm:size=2g,mode=1777
    networks:
      dokploy-network:
        ipv4_address: 10.0.1.156
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '2'
        reservations:
          memory: 768M
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

  acestream-proxy:
    image: openresty/openresty:alpine
    container_name: acestream-proxy
    restart: unless-stopped
    volumes:
      - ./nginx.conf:/usr/local/openresty/nginx/conf/nginx.conf:ro
    depends_on:
      - acestream-arm
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"
      - "traefik.http.routers.acestream-secure.rule=Host(`acestream.walerike.com`)"
      - "traefik.http.routers.acestream-secure.entrypoints=websecure"
      - "traefik.http.routers.acestream-secure.tls=true"
      - "traefik.http.routers.acestream-secure.tls.certresolver=letsencrypt"
      - "traefik.http.services.acestream-secure.loadbalancer.server.port=80"
      - "traefik.http.routers.acestream-web.rule=Host(`acestream.walerike.com`)"
      - "traefik.http.routers.acestream-web.entrypoints=web"
      - "traefik.http.routers.acestream-web.middlewares=redirect-to-https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.permanent=true"
    networks:
      - dokploy-network

  # Watchdog para limpiar sesiones zombies
  acestream-watchdog:
    image: alpine:latest
    container_name: acestream-watchdog
    restart: unless-stopped
    depends_on:
      - acestream-arm
    networks:
      - dokploy-network
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command: >
      sh -c "
      apk add --no-cache docker-cli curl &&
      while true; do
        echo '[Watchdog] Verificando estado de Acestream...';

        # Verificar si hay sesiones activas
        ACTIVE_SESSIONS=$$(docker exec acestream-arm ps aux | grep -c 'acestream' || echo '0');
        echo '[Watchdog] Sesiones activas: '$$ACTIVE_SESSIONS;

        # Si hay muchas sesiones o problemas, reiniciar
        if [ $$ACTIVE_SESSIONS -gt 50 ]; then
          echo '[Watchdog] ⚠️ Demasiadas sesiones, reiniciando...';
          docker restart acestream-arm;
          sleep 60;
        fi;

        # Cada 10 minutos, limpiar cache si está lleno
        CACHE_USAGE=$$(docker exec acestream-arm df /dev/shm | tail -1 | awk '{print $$5}' | sed 's/%//' || echo '0');
        if [ $$CACHE_USAGE -gt 90 ]; then
          echo '[Watchdog] 🧹 Cache lleno ($$CACHE_USAGE%), limpiando...';
          docker exec acestream-arm sh -c 'rm -rf /dev/shm/.ACEStream/cache/*' || true;
        fi;

        sleep 300;
      done
      "





#  acestream-arm2:
#    image: jopsis/acestream:arm64
#    container_name: acestream-arm2
#    platform: linux/arm64          # <-- Emulación ARM64 en host AMD64
#    privileged: true
#    restart: unless-stopped
#    ports:
#      - "6878:6878"                # AceStream engine HTTP
#      - "8621:8621"                # AceStream web interface
#    environment:
#      - TZ=Europe/Madrid
#      - ACESTREAM_REMOTE=yes        # Permite acceso remoto
#    volumes:
#      - ./acestream-config:/acestream.engine

volumes:
  acestream-config:

networks:
  dokploy-network:
    external: true
