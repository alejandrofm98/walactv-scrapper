services:
  walactv-proxificaurl:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    command: [ "python", "proxificaUrl.py" ]
    env_file:
      - path: .env

  walactv-new-scrapper:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    command: [ "python", "main2.py" ]
    env_file:
      - path: .env

  walactv-futboltv:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    command: [ "python", "main.py" ]
    env_file:
      - path: .env

  #PARA LOCAL
#  acestream-http-proxy:
#    image: ghcr.io/martinbjeldbak/acestream-http-proxy
#    container_name: acestream-http-proxy
#    ports:
#      - 6878:6878
#    environment:
#      - ALLOW_REMOTE_ACCESS=yes

  acestream-arm:
    image: jopsis/acestream:arm64
    container_name: acestream-arm
    privileged: true
    restart: unless-stopped
    platform: linux/arm64
    hostname: acestream-arm
    ports:
      - "6878:6878"
    environment:
      TZ: Europe/Madrid
      ALLOW_REMOTE_ACCESS: "yes"
      ACESTREAM_CACHE_SIZE: "2048M"  # Ajustado al tmpfs
      ACESTREAM_MAX_PEERS: "50"      # Aumentado
      ACESTREAM_DEBUG: "0"
      ACESTREAM_MAX_CONNECTIONS: "300"  # Aumentado
      ACESTREAM_MAX_UPLOAD_SLOTS: "15"
      ACESTREAM_UPLOAD_RATE_LIMIT: "5M"  # Aumentado
      ACESTREAM_DOWNLOAD_RATE_LIMIT: "0"
      ACESTREAM_LIVE_BUFFER_SIZE: "40"   # Aumentado
      ACESTREAM_DISK_CACHE_LIMIT: "4096M"  # Ajustado
      ACESTREAM_MEMORY_CACHE_LIMIT: "2048M"  # Ajustado al tmpfs
      ACESTREAM_PORT: "6878"
      ACESTREAM_STATE_DIR: "/dev/shm/.ACEStream"
    volumes:
      - acestream-config:/acestream.engine
    tmpfs:
      - /dev/shm:size=4g,mode=1777  # Aumentado a 4GB
    networks:
      dokploy-network:
        ipv4_address: 10.0.1.156
    deploy:
      resources:
        limits:
          memory: 4G      # Aumentado
          cpus: '2.5'     # Aumentado
        reservations:
          memory: 1G      # Aumentado
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    healthcheck:
      test: [ "CMD-SHELL", "pgrep -f acestream || exit 1" ]  # Cambio: verifica proceso
      interval: 30s      # Menos agresivo
      timeout: 10s
      retries: 3
      start_period: 90s  # Más tiempo de inicio
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  acestream-proxy:
    image: openresty/openresty:alpine
    container_name: acestream-proxy
    restart: unless-stopped
    volumes:
      - ./nginx.conf:/usr/local/openresty/nginx/conf/nginx.conf:ro
    depends_on:
      acestream-arm:
        condition: service_started
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"
      - "traefik.http.routers.acestream-secure.rule=Host(`acestream.walerike.com`)"  # Corregido: faltaba backtick
      - "traefik.http.routers.acestream-secure.entrypoints=websecure"
      - "traefik.http.routers.acestream-secure.tls=true"
      - "traefik.http.routers.acestream-secure.tls.certresolver=letsencrypt"
      - "traefik.http.services.acestream-secure.loadbalancer.server.port=80"
      - "traefik.http.routers.acestream-web.rule=Host(`acestream.walerike.com`)"  # Corregido
      - "traefik.http.routers.acestream-web.entrypoints=web"
      - "traefik.http.routers.acestream-web.middlewares=redirect-to-https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.permanent=true"
    networks:
      - dokploy-network




#  acestream-arm2:
#    image: jopsis/acestream:arm64
#    container_name: acestream-arm2
#    platform: linux/arm64          # <-- Emulación ARM64 en host AMD64
#    privileged: true
#    restart: unless-stopped
#    ports:
#      - "6878:6878"                # AceStream engine HTTP
#      - "8621:8621"                # AceStream web interface
#    environment:
#      - TZ=Europe/Madrid
#      - ACESTREAM_REMOTE=yes        # Permite acceso remoto
#    volumes:
#      - ./acestream-config:/acestream.engine

volumes:
  acestream-config:

networks:
  dokploy-network:
    external: true
