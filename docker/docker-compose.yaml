services:
  walactv-proxificaurl:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    command: [ "python", "proxificaUrl.py" ]
    env_file:
      - path: .env

  walactv-new-scrapper:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    command: [ "python", "main2.py" ]
    env_file:
      - path: .env

  walactv-futboltv:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    command: [ "python", "main.py" ]
    env_file:
      - path: .env

  #PARA LOCAL
  #  acestream-http-proxy:
  #    image: ghcr.io/martinbjeldbak/acestream-http-proxy
  #    container_name: acestream-http-proxy
  #    ports:
  #      - 6878:6878
  #    environment:
  #      - ALLOW_REMOTE_ACCESS=yes


  acestream-arm:
    image: jopsis/acestream:arm64
    container_name: acestream-arm
    hostname: acestream-arm
    privileged: true
    restart: unless-stopped
    platform: linux/arm64
    environment:
      TZ: Europe/Madrid
      ALLOW_REMOTE_ACCESS: "yes"
    volumes:
      - ./acestream-config:/acestream.engine
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"

      # HTTPS Router
      - "traefik.http.routers.acestream-secure.rule=Host(`acestream.walerike.com`)"
      - "traefik.http.routers.acestream-secure.entrypoints=websecure"
      - "traefik.http.routers.acestream-secure.tls=true"
      - "traefik.http.routers.acestream-secure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.acestream-secure.service=acestream-service"
      - "traefik.http.routers.acestream-secure.middlewares=acestream-headers"

      # HTTP Router
      - "traefik.http.routers.acestream-web.rule=Host(`acestream.walerike.com`)"
      - "traefik.http.routers.acestream-web.entrypoints=web"
      - "traefik.http.routers.acestream-web.middlewares=acestream-redirect"
      - "traefik.http.routers.acestream-web.service=acestream-service"

      # Service
      - "traefik.http.services.acestream-service.loadbalancer.server.port=6878"
      - "traefik.http.services.acestream-service.loadbalancer.server.scheme=http"
      - "traefik.http.services.acestream-service.loadbalancer.passhostheader=true"

      # Middleware de redirección
      - "traefik.http.middlewares.acestream-redirect.redirectscheme.scheme=https"
      - "traefik.http.middlewares.acestream-redirect.redirectscheme.permanent=true"

      # Middleware de headers
      - "traefik.http.middlewares.acestream-headers.headers.customrequestheaders.X-Forwarded-Proto=https"
      - "traefik.http.middlewares.acestream-headers.headers.customrequestheaders.X-Forwarded-Host=acestream.walerike.com"
      - "traefik.http.middlewares.acestream-headers.headers.customrequestheaders.X-Forwarded-Port=443"
      - "traefik.http.middlewares.acestream-headers.headers.customresponseheaders.Access-Control-Allow-Origin=*"

    networks:
      dokploy-network:
        aliases:
          - acestream-arm




#  acestream-arm2:
#    image: jopsis/acestream:arm64
#    container_name: acestream-arm2
#    platform: linux/arm64          # <-- Emulación ARM64 en host AMD64
#    privileged: true
#    restart: unless-stopped
#    ports:
#      - "6878:6878"                # AceStream engine HTTP
#      - "8621:8621"                # AceStream web interface
#    environment:
#      - TZ=Europe/Madrid
#      - ACESTREAM_REMOTE=yes        # Permite acceso remoto
#    volumes:
#      - ./acestream-config:/acestream.engine

volumes:
  acestream-config:

networks:
  dokploy-network:
    external: true
